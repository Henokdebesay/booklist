let submit = document.getElementById("submit");
let inputs = document.getElementById("inputs")
let bookTitle = inputs.querySelectorAll('input')[0];
let author = inputs.querySelectorAll('input')[1];
console.log(author);

